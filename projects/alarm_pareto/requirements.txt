# Packages this project needs. Exact pinned versions. Not ranges. This tool must
# build the same way in eighteen months. If IT gives you wheel files with
# different versions, edit the numbers here to match those wheels, then keep this
# file with the project.
#
# This file is self-contained on purpose. It ships inside the download package,
# where it is the only requirements file present.
#
# Install from local wheels like this (offline):
#   pip install --no-index --find-links C:\path\to\wheels -r requirements.txt
#
# What each one is for:
#   pandas, numpy    read and reshape the alarm log table
#   openpyxl         write the Excel workbook with native charts
#   python-pptx      write the PowerPoint deck
#   matplotlib       draw the chart pictures that go onto the slides
#   pytest           run the tests
#
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
python-pptx==1.0.2
matplotlib==3.9.2
pytest==8.3.2
